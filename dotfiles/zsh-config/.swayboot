#----------------------------------------
# Sway boot from TTY1
#----------------------------------------

if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then
	XKB_DEFAULT_LAYOUT=us exec sway -d 2> ~/.logs/sway.log
fi
