#!/bin/sh

export MOZ_DBUS_REMOTE=1 MOZ_DISABLE_CONTENT_SANDBOX=1 MOZ_DISABLE_RDD_SANDBOX=1

if [ -n "${WAYLAND_DISPLAY}" ]; then
    MOZ_ENABLE_WAYLAND=1 command -p waterfox "$@"
else
    MOZ_X11_EGL=1 GTK_USE_PORTAL=1 command -p waterfox "$@"
fi
