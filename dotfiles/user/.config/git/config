[core]
    excludesfile = ~/.config/git/ignore
[user]
    name = Ivan
    email = yumteej@gmail.com
[alias]
    st = status
    ci = commit
    cim = commit -m
    unstage = reset HEAD --
    rmlocalcommit = reset HEAD~
    last = log -1 HEAD
[color]
    ui = true
    status = auto
    branch = auto
    interactive = auto
    diff = auto
[credential]
    helper = cache --timeout=7200
