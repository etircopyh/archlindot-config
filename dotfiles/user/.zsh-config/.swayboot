#----------------------------------------
# Sway boot from TTY1
#----------------------------------------

if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]] && [ -f /bin/sway ]; then
    systemctl --user start sway.service
fi
